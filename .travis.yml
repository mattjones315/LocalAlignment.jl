language: julia
os:
  - linux
  - osx
julia:
  - release
  - nightly
matrix:
  allow_failures:
  - julia: nightly
notifications:
  email: false
script:
  #- if [[ -a .git/shallow ]]; then git fetch --unshallow; fi
  #- julia -e 'Pkg.clone(pwd()); Pkg.build("$name"); using Coverage; Codecov.sumbit(process_folder())'
  - julia -e 'Pkg.clone(pwd()); Pkg.build("$name"); using Coverage;'
after_success:
